name: pull-request-target

on:
  pull_request_target:
    branches:
      - main
    types:
      - closed

jobs:
  debug:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - run: |
          echo "${{ github.ref_name }}"
          echo "${{ toJSON(github.event) }}"
